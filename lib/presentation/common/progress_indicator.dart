import 'package:flutter/material.dart';

class ChickenProgressIndicator extends StatelessWidget {
  const ChickenProgressIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
